body {
    background: skyblue;
    margin: 0; /* Remove default margin */
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Full viewport height */
    color: #fff; /* Set text color for better contrast */
}

.container {
    padding: 20px;
    width: 400px;
    background: #fff;
    border-radius: 7px;
    color: #000; /* Set text color inside the container */
}

h1 {
    font-size: 1.5em; /* Adjust the size of the heading */
    text-align: center;
}

.input input {
    width: calc(100% - 20px); /* Make the input field fill the container width with some padding */
    padding: 10px;
    border-radius: 7px;
    border: 1px solid #ccc;
}

#addButton input {
    color: #fff; /* Text color inside button */
    background: skyblue; /* Background color of the button */
    border: none; /* Remove default border */
    border-radius: 7px;
    width: 100%;
    height: 50px;
    font-size: 1em;
    cursor: pointer; /* Show a pointer cursor on hover */
}

.button input:hover {
    background: #00aaff; /* Change background on hover */
}
