body {
    background: skyblue;
    margin: 0; /* Remove default margin */
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Full viewport height */
    color: #fff; /* Set text color for better contrast */
}

.container {
    padding: 20px;
    width: 400px;
    background: #fff;
    border-radius: 7px;
    color: #000; /* Set text color inside the container */
}

h1 {
    font-size: 1.5em; /* Adjust the size of the heading */
    text-align: center;
}

.first {
   width: 400px; /* Specific width for the input box */
    height: 60px; /* Specific height for the input box */
    padding: 10px;
    border-radius: 7px;
    border: 1px solid #ccc;
    box-sizing: border-box; /* Ensure padding is included in the width and height calculation */
}

/* Apply styles directly to input elements */
.custom-input {
    width: 400px; /* Specific width for the input box */
    height: 60px; /* Specific height for the input box */
    padding: 10px;
    border-radius: 7px;
    border: 1px solid #ccc;
    box-sizing: border-box; /* Ensure padding is included in the width and height calculation */
}

/* Style the button directly */
#addButton {
    color: #fff; /* Text color inside button */
    background: skyblue; /* Background color of the button */
    border: none; /* Remove default border */
    border-radius: 7px; /* Rounded corners */
    width: 150px; /* Set a fixed width for the button */ 
    height: 50px; /* Set a fixed height for the button */
    font-size: 1em; /* Font size for the button text */
    cursor: pointer; /* Show a pointer cursor on hover */
    text-align: center; /* Center the text inside the button */
    box-sizing: border-box; /* Include padding and border in element's total width and height */
}

#addButton:hover {
    background: deepskyblue; /* Change background color on hover */
}


.button input:hover {
    background: #00aaff; /* Change background on hover */
}
